from decouple import config

ADDRESS = config("ADDRESS")
PORT = config("PORT")
TS_USER = config("TS_USER")
TS_PASSWORD = config("PASSWORD")
TOKEN = config("TOKEN")
PERSISTENCE_FILE = config("PERSISTENCE_FILE")
ALLOWED_USER = config("ALLOWED_USER")
